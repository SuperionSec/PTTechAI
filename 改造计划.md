# PTTechAI v3 工程结构改造计划（修订版 v3）

## Summary

将 PTTechAI 从散乱的根目录结构重组为 deploy/ + frontend/ + backend/（含 system/ + pentest/ + common/ 三层）架构。
渗透测试代码只移动不修改内容，系统管理参考 RuoYi 标准建设。5 个阶段，13-18 天完成。

### 修订记录

- v2：采纳评审反馈，拆分 Phase 2 为 2a/2b，Phase 3 缩小为仅 Menu，新增 CI 循环依赖检测、Bug 验证步骤、回滚方案，common/core/ 重命名为 common/infra/。
- v2.1：根据实际代码库验证修正数据（2026-05-29）
  - core/ 实际 16 文件（含 __init__.py），agents/ 8 文件，tools/ 22 文件
  - prompts/ 实际 124 文件（122 .md + 2 JSON），data/ 8 文件，models/ 1 文件，wukongskill/ 8 文件
  - backend/core/ 实际 97 个 .py 文件 + 6 子目录（prompt_engine, rag, rbac, report_engine, smart_router, vuln_engine）
  - 跨层 import 实际 36 处/14 文件（原低估为 25+）
  - 测试函数实际 216 个（原误写 310）
  - Pydantic class Config 实际 10 处/8 文件
  - 确认双拦截器存在（AuthContext.tsx 3处, api.ts 4处）
  - wukongskill/ 保持在根目录（不迁移）
- v2.2：渗透模块平铺迁移策略（2026-05-29）
  - **核心原则：最小化渗透测试模块的文件结构和名称变更，降低迁移风险**
  - pentest/core/ 不再新建子目录（移除 agent/, cli/, sandbox/, llm/, knowledge/, mcp/, tools/, browser/, executor/）
  - backend/core/ 的 ~85 个 .py 文件平铺移入 pentest/core/（保持已有 5 个子目录：vuln_engine, rag, prompt_engine, smart_router, report_engine）
  - 根目录 core/ 的 ~15 个 .py 文件平铺移入 pentest/core/
  - 仅 1 处必要改名：core/report_generator.py → core_report_generator.py（与 backend/core/report_generator.py 冲突）
  - neurosploit.py 保持原名（不改 cli.py）
  - scheduler.py 归入 pentest/core/（不是 common/infra/），删除 backend/core/scheduler.py 空壳
- v3.0：域分离策略（2026-05-30）
  - **核心改进：根目录代码（域 A）和 backend 代码（域 B）在 pentest/ 下保持独立，0 改名，0 冲突**
  - 域 A（原根目录）：core/, tools/, agents/, prompts/, data/, config/, models/, reports/, custom_agents/, neurosploit.py → 整体移入 `backend/pentest/`，保持原始结构
  - 域 B（原 backend）：core/, api/, models/, schemas/, services/, prompts/ → 整体移入 `backend/pentest/backend/`，保持原始结构
  - 两个域的 core/ 目录保持独立（`pentest/core/` vs `pentest/backend/core/`），无需合并或改名
  - 所有命名冲突自然消除（report_generator.py、scheduler.py、task_library.json 等）
  - Import 路径：域 A 内部用 `backend.pentest.core.X`，域 B 内部用 `backend.pentest.backend.core.X`，跨域引用显式声明

---

## 一、现状问题

- 根目录散布 neurosploit 遗留的 core/(16文件)、agents/(8文件)、tools/(22文件)、prompts/(124文件: 122 .md + 2 JSON)、data/(8文件)、models/(1文件)、config/(3文件)、custom_agents/(2文件) 等
- backend/api/v1/ 有 26 个 .py 文件（含 __init__.py + routes.py），system 和 pentest 混合
- backend/core/ 有 97 个 .py 文件 + 6 子目录（prompt_engine, rag, rbac, report_engine, smart_router, vuln_engine），大量渗透引擎代码
- backend/ 中有 36 处/14 文件跨层 import 根目录 core/
- 3 处潜在 Bug：agent_orchestrator.py、md_agent.py、specialist_agents.py import from core.agent_base，但根目录 core/agent_base.py 不存在（只有 backend/core/ 下有）
- 权限系统双重（permissions.py 旧 + resource_guard.py 新）
---

## 二、依赖链深度分析

### 2.1 跨层依赖清单（backend -> 根目录 core/）

实际统计：36 处/14 文件

> **v3 说明**：backend 代码（域 B）移入 `pentest/backend/`，根目录 core/（域 A）移入 `pentest/core/`。域 B 引用域 A 时路径为 `backend.pentest.core.X`。

| 文件（移入后位置） | Import 数量 | 目标新路径 |
|------|------------|------------|
| pentest/backend/api/v1/agent.py | 1 (container_pool) | backend.pentest.core.container_pool |
| pentest/backend/api/v1/mcp.py | 2 (mcp_server, mcp_client) | backend.pentest.core.mcp_server, backend.pentest.core.mcp_client |
| pentest/backend/api/v1/sandbox.py | 5 (container_pool x5) | backend.pentest.core.container_pool |
| pentest/backend/api/v1/terminal.py | 4 (llm_manager, sandbox_manager, container_pool x2) | backend.pentest.core.{llm_manager,sandbox_manager,container_pool} |
| pentest/backend/core/agent_orchestrator.py | 1 (agent_base) | backend.pentest.core.agent_base |
| pentest/backend/core/ai_pentest_agent.py | 1 (llm_manager) | backend.pentest.core.llm_manager |
| pentest/backend/core/autonomous_agent.py | 4 (browser_validator, sandbox_manager, container_pool, knowledge_augmentor) | backend.pentest.core.{browser_validator,sandbox_manager,container_pool,knowledge_augmentor} |
| pentest/backend/core/cli_agent_runner.py | 2 (container_pool x2) | backend.pentest.core.container_pool |
| pentest/backend/core/md_agent.py | 1 (agent_base) | backend.pentest.core.agent_base |
| pentest/backend/core/researcher_agent.py | 3 (kali_sandbox, tool_registry, sandbox_manager) | backend.pentest.core.{kali_sandbox,tool_registry,sandbox_manager} |
| pentest/backend/core/scheduler.py | 1 (scheduler) | backend.pentest.core.scheduler（域 A 实现替换 backend 空壳） |
| pentest/backend/core/specialist_agents.py | 9 (agent_base + 8 specialized modules) | backend.pentest.core.{agent_base,deep_recon,banner_analyzer,...} |
| pentest/backend/core/xss_validator.py | 1 (browser_validator) | backend.pentest.core.browser_validator |
| pentest/backend/core/report_engine/generator.py | 1 (llm_manager) | backend.pentest.core.llm_manager |

### 2.2 潜在 Bug（移动前需验证并修复）

| 文件 | 问题 |
|------|------|
| backend/core/agent_orchestrator.py | from core.agent_base — 根目录 core/agent_base.py 不存在 |
| backend/core/md_agent.py | 同上 |
| backend/core/specialist_agents.py | 同上 |
| backend/core/scheduler.py | from core.scheduler import ScanScheduler — 根目录 core/scheduler.py 与 backend/core/scheduler.py 不同 |

**验证步骤（Phase 2a 开始前必须执行）：**

`
ls core/agent_base.py 2>/dev/null || echo " NOT FOUND in root\n
---

## 三、目标目录结构

`
PTTechAI/
├── deploy/                          # Docker + compose + 部署脚本
│   ├── docker/                      # <- 原 docker/
│   ├── nginx.conf
│   ├── docker-compose.yml           # <- 原根目录
│   ├── docker-compose.lite.yml      # <- 原根目录
│   └── scripts/                     # <- 原 scripts/ + 根目录脚本
├── frontend/                        # 不动
├── wukongskill/                     # 保持根目录不动
├── backend/
│   ├── system/                      # 系统管理
│   │   ├── auth/api.py              # <- backend/api/v1/auth.py
│   │   ├── users/api.py             # <- backend/api/v1/users.py
│   │   ├── rbac/api.py              # <- permissions.py + rbac.py 合并
│   │   ├── rbac/service.py          # <- services/rbac_service.py
│   │   ├── api_keys/api.py          # <- backend/api/v1/api_keys.py
│   │   ├── menu/                    # Phase 3 新增（动态菜单）
│   │   ├── audit/                   # Phase 5 后续迭代
│   │   ├── dict/                    # Phase 5 后续迭代
│   │   ├── monitor/                 # Phase 5 后续迭代
│   │   └── notification/            # Phase 5 后续迭代
│   ├── pentest/                     # 渗透测试（两大代码域，各自保持原始结构）
│   │   ├── # ---- 域 A: 原根目录代码（保持原始结构，0 改名）----
│   │   ├── core/                    # <- 根目录 core/ (15 .py 文件)
│   │   ├── tools/                   # <- 根目录 tools/ (22 文件 + 6 子目录)
│   │   ├── agents/                  # <- 根目录 agents/ (8 文件)
│   │   ├── prompts/                 # <- 根目录 prompts/ (124 文件)
│   │   ├── data/                    # <- 根目录 data/ (8 文件)
│   │   ├── config/                  # <- 根目录 config/ (3 JSON 文件)
│   │   ├── custom_agents/           # <- 根目录 custom_agents/ (2 文件)
│   │   ├── models/                  # <- 根目录 models/ (bug-bounty zip)
│   │   ├── reports/                 # <- 根目录 reports/ (benchmark/)
│   │   ├── neurosploit.py           # <- 根目录 neurosploit.py (不改名)
│   │   ├── # ---- 域 B: 原 backend 渗透代码（保持原始结构，0 改名）----
│   │   ├── backend/
│   │   │   ├── core/                # <- backend/core/ (97 .py + 5 子目录)
│   │   │   ├── api/
│   │   │   │   ├── v1/              # <- backend/api/v1/ 渗透路由 (17 文件)
│   │   │   │   └── websocket.py     # <- backend/api/websocket.py
│   │   │   ├── models/              # <- backend/models/ 渗透模型 (8 文件)
│   │   │   ├── schemas/             # <- backend/schemas/ 渗透 schema (6 文件)
│   │   │   ├── services/            # <- backend/services/ 渗透服务 (2 文件)
│   │   │   └── prompts/             # <- backend/prompts/ (1 文件)
│   ├── common/                      # 共享基础设施
│   │   ├── config.py                # <- backend/config.py
│   │   ├── db/                      # <- backend/db/
│   │   ├── models/user.py           # <- backend/models/user.py
│   │   ├── models/permission.py     # <- backend/models/permission.py
│   │   ├── schemas/auth.py          # <- backend/schemas/auth.py
│   │   ├── schemas/rbac.py          # <- backend/schemas/rbac.py
│   │   ├── infra/                   # 原 common/core/ -> 重命名避免歧义
│   │   │   ├── auth.py              # <- backend/core/auth.py
│   │   │   ├── token_manager.py
│   │   │   ├── resource_guard.py
│   │   │   ├── permissions.py
│   │   │   ├── notification_manager.py
│   │   │   └── rbac/               # <- backend/core/rbac/(4文件)
│   │   └── middleware/              # Phase 3/5 新增（审计日志等）
│   ├── scripts/                     # 初始化脚本(保留)
│   ├── migrations/                  # 数据库迁移(保留)
│   ├── tests/                       # 后端测试(保留)
│   ├── main.py
│   └── app_lifecycle.py
├── tests/                           # 项目级测试(216个)
├── .importlinter                    # CI 循环依赖检测配置
└── 改造计划.md                      # 本文件
`

---

## 四、Import 路径映射表

### 4.1 系统模块

| 旧路径 | 新路径 |
|--------|--------|
| from backend.api.v1.auth | from backend.system.auth.api |
| from backend.api.v1.users | from backend.system.users.api |
| from backend.api.v1.permissions / rbac | from backend.system.rbac.api |
| from backend.api.v1.api_keys | from backend.system.api_keys.api |
| from backend.services.rbac_service | from backend.system.rbac.service |
| from backend.schemas.auth | from backend.common.schemas.auth |
| from backend.schemas.rbac | from backend.common.schemas.rbac |

### 4.2 渗透模块

| 旧路径 | 新路径 |
|--------|--------|
| from backend.api.v1.{scans,agent,...} | from backend.pentest.api.* |
| from backend.api.websocket | from backend.pentest.api.websocket |
| from backend.models.{scan,target,...} | from backend.pentest.models.* |
| from backend.schemas.{scan,target,...} | from backend.pentest.schemas.* |
| from backend.services.{scan,report}_service | from backend.pentest.services.* |
| from backend.core.* (97个渗透文件 + 6子目录) | from backend.pentest.core.* |
| from core.* (根目录16个文件) | from backend.pentest.core.{sandbox,llm,...} |
| from agents.* (8文件) | from backend.pentest.agents.* |
| from tools.* (22文件) | from backend.pentest.tools.* |

### 4.3 共享模块

| 旧路径 | 新路径 |
|--------|--------|
| from backend.config | from backend.common.config |
| from backend.db.database | from backend.common.db.database |
| from backend.models.user | from backend.common.models.user |
| from backend.models.permission | from backend.common.models.permission |
| from backend.core.auth | from backend.common.infra.auth |
| from backend.core.token_manager | from backend.common.infra.token_manager |
| from backend.core.resource_guard | from backend.common.infra.resource_guard |
| from backend.core.permissions | from backend.common.infra.permissions |
| from backend.core.rbac.* | from backend.common.infra.rbac.* |
| from backend.core.notification_manager | from backend.common.infra.notification_manager |

### 4.4 根目录模块（域 A 内部 + 跨域引用）

> **原则：根目录代码整体移入 `backend/pentest/`，保持原始结构，0 改名。域 A 内部 import 加前缀 `backend.pentest.`，跨域引用域 B 时用 `backend.pentest.backend.core.X`。**

| 旧路径 | 新路径 | 说明 |
|--------|--------|------|
| from core.llm_manager | from backend.pentest.core.llm_manager | 域 A 内部 |
| from core.sandbox_manager | from backend.pentest.core.sandbox_manager | 域 A 内部 |
| from core.container_pool | from backend.pentest.core.container_pool | 域 A 内部 |
| from core.kali_sandbox | from backend.pentest.core.kali_sandbox | 域 A 内部 |
| from core.browser_validator | from backend.pentest.core.browser_validator | 域 A 内部 |
| from core.knowledge_augmentor | from backend.pentest.core.knowledge_augmentor | 域 A 内部 |
| from core.tool_registry | from backend.pentest.core.tool_registry | 域 A 内部 |
| from core.tool_installer | from backend.pentest.core.tool_installer | 域 A 内部 |
| from core.agent_base | from backend.pentest.core.agent_base | 域 A 内部 |
| from core.pentest_executor | from backend.pentest.core.pentest_executor | 域 A 内部 |
| from core.report_generator | from backend.pentest.core.report_generator | 域 A 内部（不改名） |
| from core.context_builder | from backend.pentest.core.context_builder | 域 A 内部 |
| from core.scheduler | from backend.pentest.core.scheduler | 域 A 内部 |
| from core.mcp_client | from backend.pentest.core.mcp_client | 域 A 内部 |
| from core.mcp_server | from backend.pentest.core.mcp_server | 域 A 内部 |
| from core.model_router | from backend.pentest.core.model_router | 域 A 内部 |
| from agents.* | from backend.pentest.agents.* | 域 A 内部 |
| from tools.* | from backend.pentest.tools.* | 域 A 内部 |
| from backend.core.X (在域 A 文件中) | from backend.pentest.backend.core.X | 域 A→域 B 跨域 |

### 4.5 内部引用更新（域 B: backend/core -> backend/pentest/backend/core）

> **原则：backend/core/ 整体移入 `backend/pentest/backend/core/`，保持原始结构（含子目录），0 改名。`from backend.core.X` 统一变为 `from backend.pentest.backend.core.X`。**

| 旧路径 | 新路径 |
|--------|--------|
| from backend.core.autonomous_agent | from backend.pentest.backend.core.autonomous_agent |
| from backend.core.agent_memory | from backend.pentest.backend.core.agent_memory |
| from backend.core.vuln_engine.* | from backend.pentest.backend.core.vuln_engine.* |
| from backend.core.response_verifier | from backend.pentest.backend.core.response_verifier |
| from backend.core.negative_control | from backend.pentest.backend.core.negative_control |
| from backend.core.proof_of_execution | from backend.pentest.backend.core.proof_of_execution |
| from backend.core.confidence_scorer | from backend.pentest.backend.core.confidence_scorer |
| from backend.core.validation_judge | from backend.pentest.backend.core.validation_judge |
| from backend.core.access_control_learner | from backend.pentest.backend.core.access_control_learner |
| from backend.core.request_engine | from backend.pentest.backend.core.request_engine |
| from backend.core.waf_detector | from backend.pentest.backend.core.waf_detector |
| from backend.core.strategy_adapter | from backend.pentest.backend.core.strategy_adapter |
| from backend.core.chain_engine | from backend.pentest.backend.core.chain_engine |
| from backend.core.auth_manager | from backend.pentest.backend.core.auth_manager |
| from backend.core.task_library | from backend.pentest.backend.core.task_library |
| from backend.core.ai_prompt_processor | from backend.pentest.backend.core.ai_prompt_processor |
| from backend.core.autonomous_scanner | from backend.pentest.backend.core.autonomous_scanner |
| from backend.core.ai_pentest_agent | from backend.pentest.backend.core.ai_pentest_agent |
| from backend.core.recon_integration | from backend.pentest.backend.core.recon_integration |
| from backend.core.report_engine.generator | from backend.pentest.backend.core.report_engine.generator |
| from backend.core.report_generator | from backend.pentest.backend.core.report_generator |
| from backend.core.rag.* | from backend.pentest.backend.core.rag.* |
| from backend.core.smart_router.* | from backend.pentest.backend.core.smart_router.* |
| from backend.core.prompt_engine.* | from backend.pentest.backend.core.prompt_engine.* |
| from backend.core.knowledge_processor | from backend.pentest.backend.core.knowledge_processor |
| from backend.core.scheduler | from backend.pentest.backend.core.scheduler |
| from backend.models.* (渗透模型) | from backend.pentest.backend.models.* |
| from backend.schemas.* (渗透 schema) | from backend.pentest.backend.schemas.* |
| from backend.services.* (渗透服务) | from backend.pentest.backend.services.* |

---

## 五、实施计划

### 回滚策略（全局）

- 所有结构改造在独立分支 refactor/v3-restructure 上进行
- 每个子步骤一个 commit（不 squash），方便 git revert 单步回滚
- Phase 2a 和 Phase 2b 各自独立 commit，出问题可分别回退
- 每个 Phase 完成后打 tag（v3-phase1, v3-phase2a, ...）
- 不直接 merge 到 main，先在 staging 环境验证 1 周

### Phase 1: 基础加固（2-3 天，不改结构）

- 安全：DEBUG 默认 False、SECRET_KEY 强制校验、sha256_crypt -> bcrypt、登录速率限制、未映射 API 策略改 warn
- Pydantic V2：10 处 class Config -> model_config = ConfigDict(...)（跨 8 个文件：config.py, schemas/agent_task.py, schemas/auth.py x2, schemas/prompt.py, schemas/report.py, schemas/scan.py, schemas/target.py, schemas/vulnerability.py x2）
- datetime：96 处 utcnow() -> now(timezone.utc)（跨 24 个文件）
- 前端：移除死依赖、删除 legacyApi.ts、合并 AuthContext.tsx 和 api.ts 中的双 axios 拦截器
- 验收：216 测试通过，0 Pydantic 警告

### Phase 2a: 共享层提取（2 天）

1. 验证 Bug 声明（执行第二章验证脚本，确认 core.agent_base 实际状态）
2. 修复已确认的 Bug（3 处 agent_base + scheduler.py）
3. 创建 backend/common/ 目录骨架（config, db, models, schemas, infra/, middleware/）
4. 移动共享模块到 common/
5. 使用 rope 或 libcst 自动化重写 common/ 相关 import
6. 更新 main.py、app_lifecycle.py 中 common 相关 import
- 验收：216 测试通过，无循环 import

### Phase 2b: 渗透层+系统层重组（2-3 天）

> **v3 核心策略："只移不改"——域 A（根目录代码）和域 B（backend 代码）在 pentest/ 下保持独立，0 改名，0 冲突。**

**T1: 移动根目录模块到 pentest/（域 A）**
- 整体移入，保持原始结构，0 改名
- core/ → pentest/core/ (15 .py)
- tools/ → pentest/tools/ (22 文件 + 6 子目录)
- agents/ → pentest/agents/ (8 文件)
- prompts/ → pentest/prompts/ (124 文件)
- data/ → pentest/data/ (8 文件)
- config/ → pentest/config/ (3 JSON)
- custom_agents/ → pentest/custom_agents/ (2 文件)
- models/ → pentest/models/ (bug-bounty zip)
- reports/ → pentest/reports/ (benchmark/)
- neurosploit.py → pentest/neurosploit.py (不改名)
- 更新域 A 内部 import：`from core.X` → `from backend.pentest.core.X`
- 更新域 A→域 B 跨域 import：`from backend.core.X` → `from backend.pentest.backend.core.X`

**T2: 移动 backend 渗透代码到 pentest/backend/（域 B）**
- 整体移入，保持原始结构，0 改名
- backend/core/ → pentest/backend/core/ (97 .py + 5 子目录)
- backend/api/ → pentest/backend/api/ (v1/ + websocket.py)
- backend/models/ → pentest/backend/models/ (8 渗透模型)
- backend/schemas/ → pentest/backend/schemas/ (6 渗透 schema)
- backend/services/ → pentest/backend/services/ (2 渗透服务)
- backend/prompts/ → pentest/backend/prompts/ (1 文件)
- 更新域 B 内部 import：`from backend.core.X` → `from backend.pentest.backend.core.X`（~220 处）
- 更新域 B→域 A 跨域 import：`from core.X` → `from backend.pentest.core.X`（~36 处）

**T3: 创建 system/ 并移动系统管理模块**
- auth/api.py, users/api.py, rbac/api.py + service.py, api_keys/api.py, system/api.py
- 从 pentest/backend/api/v1/ 中提取系统路由到 system/

**T4: 更新入口文件 + routes.py**
- routes.py 移到 backend/routes.py，更新所有 import
- main.py, app_lifecycle.py, conftest.py, test_backend_imports.py 更新 import

**T5: 部署文件 + 清理**
- docker/, compose, scripts → deploy/
- 更新 Dockerfile COPY 路径
- 清理空目录
- 添加 .importlinter 配置

- 验收：216 测试通过，渗透功能正常，无 `from backend\.core\.` / `from core\.` / `from agents\.` / `from tools\.` 残留

**自动化 Import 重写工具选择：**

| 工具 | 优点 | 缺点 | 推荐场景 |
|------|------|------|----------|
| rope | 成熟的 Python 重构库，支持 rename/move module | 对动态 import 支持有限 | 批量 module move |
| libcst | AST 级别精确控制，保留注释和格式 | 学习曲线陡 | 复杂替换规则 |
| sed + 正则 | 简单快速 | 易误匹配，不处理 multi-line | 简单文本替换兜底 |

推荐组合：rope 做 module move + libcst 处理剩余 edge case + sed 兜底简单替换。

### Phase 3: 动态菜单（3-4 天）

仅实现动态菜单，其余系统管理模块推到 Phase 5。

- 数据模型 Menu：id, parent_id, name, path, component, icon, sort_order, permission, is_visible, is_active
- API：GET/POST/PUT/DELETE /api/v1/system/menus，GET /api/v1/system/menus/user
- 前端 MenuManagementPage.tsx：树形展示、增删改查、拖拽排序、权限绑定
- 迁移：将 FRONTEND_ROUTES 硬编码数据导入数据库
- 验收：动态菜单配置实时生效，前端菜单从数据库加载

### Phase 4: 集成测试与优化（2-3 天）

- 结构改造回归测试（import 完整性、API 路由覆盖）
- 动态菜单 API 测试 + E2E 测试
- 数据库索引优化、菜单查询缓存
- 文档更新
- 验收：测试覆盖率 80%+，文档完整

### Phase 5: 系统管理扩展（后续迭代，独立项目）

在 Phase 1-4 稳定运行后，按需启动：

- 审计日志：AuditLog 模型 + 中间件 + 查询 API + 列表前端
- 数据字典：DictType + DictData 模型 + CRUD + 前端
- 系统监控：服务器/应用/扫描/容器状态 API + 仪表盘前端
- 通知管理：Notification 模型 + API + 铃铛组件
- 各模块独立评估、独立排期、独立验收

---

## 六、风险分析

| 风险 | 缓解措施 |
|------|----------|
| 大规模文件移动导致 import 错误 | 使用 sed 批量机械替换 import 前缀，每步运行测试 |
| 渗透测试功能受损 | Phase 2b 不修改任何文件内容，只移动+更新 import（0 改名，0 重组） |
| 命名冲突（report_generator.py, scheduler.py 等） | **v3 已消除**：域 A 和域 B 保持独立，不合并 |
| 循环 import | system/pentest 禁止互引，CI 加 import-linter 强制检测 |
| 前端 API 路径变化 | 所有 /api/v1/... 路由前缀保持不变 |
| Docker 构建失败 | 更新 Dockerfile COPY 路径后验证构建 |
| 数据库迁移链断裂 | migrations/ 整体保留不动 |
| Phase 2 中途严重问题 | 独立分支 + 每步一 commit + tag 标记，可 git revert 回滚 |

### CI 循环依赖检测

项目根目录新增 .importlinter 配置：

`
# .importlinter
[importlinter]
root_package = backend

[importlinter:contract:system-pentest-isolation]
name = system and pentest must not import each other
type = independence
modules =
    backend.system
    backend.pentest

[importlinter:contract:common-direction]
name = system and pentest may import common, not vice versa
type = layers
layers =
    backend.system | backend.pentest
    backend.common
`

CI 流水线增加：lint-imports（import-linter CLI 命令），失败则阻断合并。

---

## 七、时间线总结

| 阶段 | 工作内容 | 预计时间 | 验收标准 |
|------|----------|----------|----------|
| Phase 1 | 安全加固、Pydantic V2(10处)、datetime(96处)、前端清理 | 2-3 天 | 216 测试通过，0 警告 |
| Phase 2a | Bug 验证修复、common/ 共享层提取 | 2 天 | 216 测试通过 |
| Phase 2b | pentest/ + system/ 重组（域 A + 域 B 独立移入，0 改名）、CI 规则 | 2-3 天 | 216 测试通过，无残留 import |
| Phase 3 | 动态菜单（Menu 模块） | 3-4 天 | Menu API + 前端可用 |
| Phase 4 | 集成测试、性能优化、文档更新 | 2-3 天 | 测试覆盖率 80%+ |
| Phase 5 | 审计/字典/监控/通知（后续独立迭代） | 按需 | 各模块独立验收 |

**Phase 1-4 总计：11-15 天**（Phase 5 另行排期）

---

## 八、关键原则

1. **渗透测试代码零修改**：只移动位置，不改内容，不改文件名，只更新 import 路径
2. **域分离策略（v3 新增）**：根目录代码（域 A）和 backend 代码（域 B）在 pentest/ 下保持独立，0 改名，0 冲突，命名冲突自然消除
3. **API 路径不变**：所有 /api/v1/* 路由前缀保持原样，前端无需改动
4. **system/pentest 禁止互引**：通过 common/ 共享基础设施，CI 强制检测
5. **每阶段独立验证**：每个 Phase 完成后立即运行测试，确保可独立交付
6. **潜在 Bug 先验证再修复**：执行前跑验证脚本，避免误判
7. **自动化优先**：import 重写使用 sed 批量机械替换，不手动逐文件处理
8. **可回滚**：独立分支、每步一 commit、打 tag，出问题可精确 revert
